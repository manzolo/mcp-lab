# Database Configuration
POSTGRES_DB=mcp
POSTGRES_USER=mcp
POSTGRES_PASSWORD=mcp

# Ollama Configuration
# Use http://ollama:11434 for internal Docker networking (if using local-llm profile)
# Use http://host.docker.internal:11434 if connecting to host machine
OLLAMA_URL=http://ollama:11434
MODEL_NAME=llama3.2:3b
