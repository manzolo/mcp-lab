# MCP Agent Dependencies
# ======================
# Pinned versions for reproducibility and educational stability

# MCP Python SDK - Official Model Context Protocol implementation
# Provides client session management and transport protocols
mcp[cli]>=1.0.0

# HTTP Requests - Used to communicate with Ollama LLM
requests==2.31.0
urllib3==2.1.0

# Async HTTP client for MCP SDK (required for streamable-http transport)
httpx>=0.24.0
httpx-sse>=0.4.0
